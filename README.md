# VK Cloud Network Interface Manager (Multi-threaded)

**Высокопроизводительный инструмент для автоматического поиска "красивых" или "чистых" IP-адресов в VK Cloud (MCS).**

## 🚀 Особенности

- **Многопоточность:** Работает в несколько потоков (воркеров) одновременно, максимально утилизируя квоту портов.
- **Конвейерная обработка:** Не ждет завершения всей пачки. Как только один порт проверен и не подошел — он удаляется, и на его место тут же создается новый.
- **Агрессивная очистка:** Автоматически удаляет "висячие" порты и лишние интерфейсы с ВМ при старте и сбоях.
- **Защита основного IP:** Параметр `SAFE_IP` гарантирует, что ваш основной интерфейс управления никогда не будет удален.
- **Telegram-уведомления:** Присылает сообщение, когда нужный IP найден.

## 📋 Алгоритм работы

1. Скрипт проверяет квоту и очищает "мусор" (старые порты), оставляя только `SAFE_IP`.
2. Запускается пул из `NUM_PORTS` потоков.
3. Каждый поток:
   - Создает порт во внешней сети.
   - Подключает его к вашей ВМ.
   - Ждет получения IP.
   - Проверяет IP на вхождение в заданные диапазоны.
   - **Если IP не подходит:** тут же отключает и удаляет порт, освобождая слот.
   - **Если IP подходит:** останавливает все остальные потоки, отправляет уведомление и завершает работу, оставляя порт активным.

## 📦 Требования

- Python 3.8+
- Аккаунт VK Cloud
- Существующая Виртуальная Машина (ВМ)

## 🛠 Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/eskievv/vk_cloud_interface_manager.git
   cd vk_cloud_interface_manager
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройте окружение:**
   Скопируйте пример конфигурации:
   ```bash
   cp env.example .env
   ```
   Отредактируйте `.env`, указав:
   - `VK_CLOUD_AUTH_TOKEN`: Ваш API токен.
   - `VM_ID`: ID вашей виртуальной машины.
   - `SAFE_IP`: Ваш текущий IP, который нельзя удалять.
   - `NUM_PORTS`: Количество потоков (обычно Квота - 1).

## 🚦 Запуск

**Проверка соединения:**
Рекомендуется запустить перед началом работы, чтобы убедиться в правильности настроек.
```bash
python test_connection.py
```

**Запуск поиска:**
```bash
python vk_cloud_interface_manager.py
```

## ⚠️ Важно

- **Токен:** API токен VK Cloud живет ограниченное время (обычно до 24 часов). Если вы видите ошибку `401 Unauthorized`, обновите токен в `.env`.
- **Квоты:** Убедитесь, что параметр `NUM_PORTS` не превышает вашу квоту на внешние порты (минус 1 для основного IP). Иначе вы получите ошибку `409 Conflict`.

## 🔧 Конфигурация диапазонов

Диапазоны задаются в `.env`:

```ini
IP_RANGE_1_START=95.163.248.10
IP_RANGE_1_END=95.163.251.250
```

Скрипт преобразует IP в числовые значения и проверяет вхождение.

## 📝 Лицензия

MIT License